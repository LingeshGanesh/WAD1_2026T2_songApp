<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Album</title>
</head>

<body>
    <h1>Edit Album</h1>
    <button type="button"><strong><a href="/album/<%= album._id %>">Back To Album</a></strong></button><br><br>

    <form action="/album/edit/<%= album._id %>" method="post">
        <label>Title:</label>
        <input type="text" name="title" value="<%= album.title %>" required><br><br>

        <label>Description:</label>
        <textarea name="description"><%= album.description %></textarea><br><br>

        <label>Release Year:</label>
        <input type="number" name="year" value="<%= album.yearReleased %>" required><br><br>

        <label>Songs:</label><br>
        <input type="text" id="songSearch" placeholder="Search by song title..." autocomplete="off"
            style="width:200px"><br>
        <ul id="songResults"
            style="list-style:none; padding:0; margin:0; border:1px solid #ccc; width:200px; display:none;"></ul>
        <div id="selectedSongs"></div>
        <input type="hidden" name="songs" id="songsHidden"><br>

        <button type="submit">Save Changes</button>
    </form>

    <% if(msg !="" ) { %>
        <p><strong><%= msg %></strong></p>
    <% } %>

    <script>
        window.__initialSongs__ = JSON.parse(`<%- JSON.stringify(album.songs.map(s => ({ _id: s._id.toString(), title: s.title, artist: s.artist }))) %>`);
    </script>
    <script src="/javascript/song-search.js"></script>
</body>

</html>
